Files placed in this directory are bind-mounted at /data-host-share
inside the containers launched by bin/* (per their mounts.*.txt).
Use it to share host files into the container without rebuilding.
